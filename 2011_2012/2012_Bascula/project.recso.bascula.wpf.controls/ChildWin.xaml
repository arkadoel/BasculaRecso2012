<UserControl x:Class="project.recso.bascula.wpf.controls.ChildWin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="300" Width="300" Background="{x:Null}" Unloaded="UserControl_Unloaded">
    <Grid Name="Ventana"
        MouseDown="Ventana_MouseDown" MouseMove="Ventana_MouseMove" MouseUp="Ventana_MouseUp">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="tt"/>
        </Grid.RenderTransform>
        <Rectangle Margin="5,5,5,5" Fill="Gainsboro" Stroke="Black" StrokeThickness="3" RadiusX="10" RadiusY="10">
            <Rectangle.Effect>
                <DropShadowEffect BlurRadius="10" Direction="0" ShadowDepth="2" />
            </Rectangle.Effect>
        </Rectangle>
        <Image Name="Cerrar" Margin="0,3,3,0" MouseLeftButtonDown="image1_MouseLeftButtonDown" Height="27" VerticalAlignment="Top" HorizontalAlignment="Right" Width="26" Source="/project.recso.bascula.wpf.controls;component/Images/redbutton.png"></Image>
    </Grid>
        
</UserControl>
